import styles from './LogoSwitch.module.css';

const LogoSwitch = () => {
  return (
    <div className={styles.container}>
        {/* Add the Logo and the switch button here */}
    </div>
  );
};

export default LogoSwitch;